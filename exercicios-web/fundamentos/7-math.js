const raio = 5.6
const area = Math.PI * Math.pow(raio, 2) //Math.PI = pi, Math.pow() = exponenciacao

console.log(area.toFixed(3))
console.log(typeof Math)

let y = 2**8 // Agora há o operador de exponenciação
console.log(y)